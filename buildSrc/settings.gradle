pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version "${kotlin_version}"
        id 'org.jetbrains.kotlin.plugin.serialization' version "${kotlin_version}"
    }
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'defcon-buildsrc'
